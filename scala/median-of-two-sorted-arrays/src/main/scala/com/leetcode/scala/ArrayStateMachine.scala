package com.leetcode.scala;

import scala.collection.mutable.ArrayBuffer

import com.leetcode.scala.Median
import com.leetcode.scala.Section

class MedianOfTwoSortedArray(array: Array[Int]) {
  private val _arrayBuffer = new ArrayBuffer()
  private val _range = new Section()
  private val _median = new Median()
}

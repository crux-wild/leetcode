package com.leetcode.scala;

class Section {
  private var _start = 0
  private var _end = 0

  def start = _start
  def start_= (newStart: Int): Unit = {
    //if (_start <= _end) {
      //_start = newStart
    //} else {
      //// @TODO 操作处理
    //}
  }

  def end = _end
  def end_= (newEnd: Int): Unit = {
    //if (_start <= _end) {
      //_end = newEnd
    //} else {
      //// @TODO 操作处理
    //}
  }
}
